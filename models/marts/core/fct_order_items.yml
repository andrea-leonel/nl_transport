models:
  - name: fct_order_items
    config:
      contract:
        enforced: true
      columns:
        - name: id
          data_type: varchar
          constraints: 
            - type: not_null
        - name: order_id
          data_type: varchar
          constraints: 
            - type: not_null
        - name: sku
          data_type: varchar
          constraints: 
            - type: not_null